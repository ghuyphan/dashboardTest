import { FlyoutDirective } from './flyout.directive';

describe('FlyoutDirective', () => {
  it('should create an instance', () => {
    const directive = new FlyoutDirective();
    expect(directive).toBeTruthy();
  });
});
